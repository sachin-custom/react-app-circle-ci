version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:20
    
    steps:
      - checkout
      - run: 
          command: "echo hello world"

workflows:
  myworkflow:
    jobs:
      - build